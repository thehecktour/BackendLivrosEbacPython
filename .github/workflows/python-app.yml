name: FastAPI - Basic Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  fastapi-tests:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout do código
        uses: actions/checkout@v3

      - name: 🐍 Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: 🛠️ Instalar Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: 📦 Instalar dependências com Poetry
        run: poetry install --no-interaction --no-root

      - name: 🧪 Rodar testes (placeholder)
        run: echo "Nenhum teste configurado ainda."

      - name: 🗃️ Verificar conexão com banco de dados
        env:
          DATABASE_URL: "sqlite:///./test.db"
        run: |
          python -c "from sqlalchemy import create_engine; engine = create_engine('${{ env.DATABASE_URL }}'); conn = engine.connect(); print('Conexão bem-sucedida!'); conn.close()"
